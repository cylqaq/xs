# 会话接力包（Session Relay）

> 由 `pnpm forge relay refresh` 生成。新会话请先读本文。

## 本书快照

（自动生成）

## 生产约束

见 `state/writing-plan.json`：`maxChaptersPerSession` 默认 4。

## 下一步

运行 `pnpm forge next stories/{id}`

## 待作者确认

见 `state/working/session-collect.yaml`
