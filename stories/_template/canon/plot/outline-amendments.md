# 大纲补丁日志（append-only）

> **禁止**改写 `outline.md` 正文来吸收火花。宏观结构调整只在此 **追加** 记录，并须 `author_ack` + `plot-architect` 复核。  
> 来源：`registries/narrative-sparks.yaml` 中 `impact_tier: outline` 且 `status: promoted` 的条目。

## 格式

```markdown
### YYYY-MM-DD · nk-xxx · 标题
- **触发章**：N
- **变更意图**：一句话
- **影响范围**：幕/卷/弧
- **author_ack**：true
```

## 条目

（暂无）
